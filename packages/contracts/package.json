{
  "name": "@lobster/contracts",
  "version": "1.0.0",
  "description": "Lobster Battle Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "dev": "hardhat node",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network polygonMumbai",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network polygon",
    "verify": "hardhat verify --network polygon",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "hardhat-deploy": "^0.11.0",
    "dotenv": "^16.3.0"
  }
}
